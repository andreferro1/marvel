import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:marvel_vrsoft/app/modules/comic/comic_page.dart';

void main() {
  testWidgets('ComicPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ComicPage(title: 'Comic')));
    //  final titleFinder = find.text('Comic');
    //  expect(titleFinder, findsOneWidget);
  });
}
